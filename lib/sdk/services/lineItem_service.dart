abstract class LineItemService {
  Future<void> createLineItem(String productName, double quantity, double mrp);
  // Future<List<LineItem>> getAllTodos();
  // Future<LineItem> getTodo(String id);
}
